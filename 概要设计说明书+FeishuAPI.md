# 办公场景驱动的智能知识助手需求概要说明书

## 1. 项目名称
- 办公场景驱动的智能知识助手

## 2. 项目定位
- 本项目不是单纯的飞书聊天 Bot，也不是与飞书松散集成的外部工具，而是一个以飞书办公场景为主战场、由 Agent 驱动的主动知识服务系统。
- 项目以飞书作为主要知识源、主要触达界面和主要执行闭环平台，围绕真实办公场景完成“知识提炼 -> 主动分发 -> 执行落地 -> 效果验证”的完整链路。
- 整体方案采用“飞书原生交互面 + 外部 Agent 编排内核”的模式：
  - 飞书内承载文档、妙记、消息、任务、日历、多维表格等数据源与结果触达。
  - 飞书外承载 Agent 编排、规则逻辑、调度、评测、日志、重试与可观测性。

## 3. 建设目标
- 建设一个面向飞书办公场景的主动知识服务 Agent 系统。
- 以 D 方向“团队待办中枢与进展自动对账”为核心能力，以 B 方向“会议与项目全链路伴侣”为首个高频入口场景。
- 实现跨文档、会议、消息、任务等来源的事项统一沉淀、自动回写、持续对账与管理洞察。
- 最终交付一个可运行的生产化最小闭环版本，并能通过准确性、接受度和效率提升指标完成价值验证。

## 4. 目标用户
- 一线协作团队：项目经理、产品经理、研发、测试、运营。
- 团队管理者：需要了解事项推进状态、阻塞风险和团队协作效率的人。
- 高频终端用户：作为 C 方向扩展场景的研发和工程团队成员。

## 5. 业务痛点
- 企业关键信息散落在飞书文档、会议妙记、群消息、任务和评论中，缺乏统一视图。
- 会后 Action Items 经常遗漏，负责人、截止时间和推进状态不清晰。
- 传统搜索和问答依赖用户主动发起，无法在正确时机主动提供高密度知识。
- 管理者难以持续掌握重点事项、阻塞原因和整体推进效率。

## 5.1 为什么通用搜索/问答无法满足

| 维度 | 通用搜索/问答 | 本系统 |
| --- | --- | --- |
| 触发模式 | 被动：用户必须知道去哪搜、搜什么关键词 | 主动：系统根据会议结束、日程变更、超期阈值等事件自动触发 |
| 知识形态 | 返回原始文档片段或通用摘要 | 返回经过提纯的结构化事项（含负责人、优先级、截止时间）和场景化知识产物 |
| 状态追踪 | 无状态：每次搜索独立，不记录历史 | 持续追踪：事项中枢保存完整生命周期，支持对账、预警和闭环 |
| 执行闭环 | 无：搜索结果需人工搬运到任务系统 | 自动创建飞书任务、绑定负责人、同步状态，形成可追踪的执行链路 |
| 上下文感知 | 无：不了解用户当前工作场景 | 基于会议主题、参与人、时间窗口做场景化推送 |
| 信息聚合 | 单源：通常只搜索一种数据源 | 跨源：聚合文档、会议、消息、任务、邮件中的信息 |
| 持续价值 | 一次性：结果用完即弃 | 累积性：事项归并、历史决策积累，系统越用越准 |

通用搜索解决的是"找到信息"，本系统解决的是"在正确时机将正确信息以可执行的形式交付给正确的人"。

## 6. 建设范围

### 6.1 核心范围
- 会议前背景知识检索与卡片推送。
- 会议后决策、待办、风险抽取。
- 事项归并去重、负责人/优先级/截止时间补齐。
- 飞书任务创建/更新与统一推进总表维护。
- 周期性管理洞察输出。

### 6.2 扩展范围
- CLI 触发的研发知识推送。
- 更多事件源，如文档编辑、消息关键词、阈值告警。

## 7. 功能需求

> 实现状态说明：本章描述最终系统需求，不表示当前 MVP 已全部完成。当前阶段主要完成会议/妙记、任务、Base、消息卡片和 OpenClaw 工具入口的雏形；Docs、Wiki、IM 内容抽取、邮件、日历会前链路、卡片回调闭环仍属于后续阶段。

### 7.1 知识接入
- 接入 Docs、Docx、Wiki、Minutes、IM、Task、Calendar、Bitable 等飞书数据源。
- 支持基于关键词、参与人、时间窗口、文档空间、会议记录的联合检索。

当前实现状态：

| 来源 | 状态 | 说明 |
| --- | --- | --- |
| Minutes / 会议纪要 | 已实现雏形 | 用于会后事项抽取 |
| Task | 已实现雏形 | 支持创建任务和查询任务状态 |
| Bitable / Base | 已实现雏形 | 支持事项总表投影 |
| IM / 卡片发送 | 部分实现 | 支持发送消息卡片，回调闭环未完成 |
| Calendar | 未完整实现 | 会前背景卡片触发待接入 |
| Docs / Wiki | 未完整实现 | 最终多源知识接入目标 |
| Mail | 未完整实现 | 最终多源知识接入目标 |

### 7.2 主动触发
- 支持会议开始、会议结束、日程变更、定时任务、文档变更等触发方式。
- 支持基于阈值和状态变化的主动提醒。

### 7.3 知识产物生成
- 会前背景卡片。
- 会后结构化摘要，包括决策、待办、风险、引用来源。
- 团队重点事项推进总表。
- 周报、周洞察和风险提示。

### 7.4 任务闭环
- 自动创建或更新飞书任务。
- 支持成员、清单、自定义字段、提醒、来源信息等写入。
- 支持事项与任务之间的双向状态同步。

### 7.5 交互确认
- 通过飞书卡片提供确认、驳回、补充字段、认领任务等能力。

### 7.6 评测与观测
- 记录每次触发、检索、抽取、写回和推送结果。
- 支持来源追踪、错误重试、幂等控制和执行日志。

## 8. 非功能需求
- 稳定性：事件消费支持重试、补偿和重复消息去重。
- 可扩展性：数据接入层、抽取层、执行层应解耦。
- 可观测性：支持日志、任务链路追踪、失败告警。
- 安全性：遵循最小权限原则，区分应用身份与用户身份。
- 可解释性：所有关键结论和任务应带来源链接。
- 生产化：避免把核心能力耦合为一次性脚本或纯演示链路。

## 9. 输入与输出

### 9.1 输入
- 飞书文档与知识库内容。
- 飞书会议妙记、纪要、逐字稿。
- 日历与会议事件。
- 群消息与历史任务。

### 9.2 输出
- 飞书消息卡片。
- 结构化报告文档。
- 飞书任务记录。
- 多维表格记录。
- CLI 提示。

## 10. 触发方式
- 定时触发：每日或每周汇总、定时巡检、周期性洞察。
- 事件驱动：会议开始、会议结束、日程变更、文档编辑。
- 阈值触发：事项长期未推进、即将超期、阻塞未解除。
- 人工触发：命令行、消息指令、卡片按钮。

## 11. 成功指标
- 准确性：决策、待办、负责人、截止时间抽取准确率。
- 接受度：卡片点击率、任务认领率、任务完成率。
- 效率提升：会后整理耗时下降、事项漏记率下降、周报整理成本下降。
- 运行质量：成功执行率、补偿成功率、重复写入率、推送成功率。

## 11.1 评测用例设计

### 准确性评测

| 编号 | 评测维度 | 用例描述 | 输入 | 期望输出 | 评分方式 |
| --- | --- | --- | --- | --- | --- |
| E1 | 事项抽取准确率 | 从 3 场真实会议纪要中抽取 Action Items | 会议纪要全文 | 人工标注的标准事项列表 | Precision / Recall / F1 |
| E2 | 事项类型分类 | 判断抽取的事项是 todo / decision / risk / blocker | E1 的抽取结果 | 人工标注的类型标签 | 分类准确率 |
| E3 | 负责人推断 | 对抽取的事项推断 owner | 事项 + 会议参与人列表 | 人工标注的负责人 | 准确率 |
| E4 | 截止时间推断 | 对抽取的事项推断 dueAt | 事项 + 会议上下文 | 人工标注的截止日期 | 准确率（允许 ±1 天误差） |
| E5 | 事项归并判断 | 将来自多次会议的相同事项归并 | 新事项 + 已有事项库 | 人工标注的归并关系 | 归并准确率 / 误合并率 |

### 接受度评测

| 编号 | 评测维度 | 用例描述 | 数据来源 | 评分方式 |
| --- | --- | --- | --- | --- |
| E6 | 卡片确认率 | 统计会后确认卡片中事项的确认/驳回/修改比例 | push_records + 回调日志 | 确认率 = 确认数 / 总事项数 |
| E7 | 任务认领率 | 统计风险预警卡片中事项的认领比例 | push_records + 回调日志 | 认领率 = 认领数 / 推送数 |

### 效率提升评测

| 编号 | 评测维度 | 用例描述 | 基线 | 评分方式 |
| --- | --- | --- | --- | --- |
| E8 | 会后整理耗时 | 对比人工整理 vs 系统自动整理 Action Items 的耗时 | 人工记录整理时间 | 时间下降比 |
| E9 | 事项漏记率 | 对比系统抽取结果与人工全量标注，统计遗漏比例 | 人工全量标注 | 漏记率 = 遗漏数 / 标准总数 |
| E10 | 来源可追溯性 | 检查每条事项是否附带正确的来源链接 | 人工验证 | 来源覆盖率 |

### 评分标准

- **事项抽取 F1 目标**：>= 0.75（首版）
- **负责人推断准确率目标**：>= 0.70
- **事项归并准确率目标**：>= 0.80，误合并率 < 0.05
- **卡片确认率目标**：>= 0.60（低于此值说明抽取质量不足）
- **会后整理耗时下降目标**：>= 50%

### 评测执行方案
1. 选取 3-5 场不同规模和主题的真实会议作为测试集
2. 由团队成员对会议纪要做人工标注，形成标准答案
3. 系统对同一批会议纪要执行自动抽取
4. 使用 `evaluation_records` 表记录每次评测的输入、输出和分数
5. 对比系统输出与人工标注，计算各项指标
6. 将结果汇总为效果验证报告

## 12. 建议实施结构
- Agent 主体：`OpenClaw`，负责理解用户意图、选择工具、组织反馈。
- 后台工作流：`Trigger.dev`，负责定时任务、补偿、可靠重试和后台对账，不作为产品主体。
- 核心主线：`D` 团队待办中枢与进展自动对账。
- 首个高频入口：`B` 会议与项目全链路伴侣。
- 管理层输出：`A` 周期性智能总结与洞察。
- 差异化扩展：`C` CLI 碎片知识推送助手。
- 能力分层：
  - 飞书 API 与 `larksuite/cli` 负责能力底座。
  - OpenClaw 负责 Agent 主体体验。
  - Node.js 业务内核负责知识提炼、事项对账、主动推送和效果验证。

## 13. 能力底座：lark-cli 安装验证与能力映射

本节将 §7 功能需求与 [larksuite/cli](https://github.com/larksuite/cli) 官方能力对齐，并记录本机可复现的验证方式，便于后续实现与联调排障。

### 13.1 本环境安装与验证（2026-04-24，Windows / PowerShell）

**前置：Node.js（npm / npx）**

- 本机已验证：Node v22.22.0、npm 11.9.0（满足官方要求）。

**安装（与官方文档一致）**

```text
npm install -g @larksuite/cli
npx skills add larksuite/cli -y -g
```

**验证结果**

- `lark-cli --version` → `1.0.18`（全局 CLI 可执行）。
- `npx skills add ...` 成功安装 23 个 Agent Skills 至本机 `~/.agents/skills/`，与仓库说明一致（含 `lark-*` 系列及 `lark-workflow-*`）。
- 在未执行过应用配置时，`lark-cli auth status` 返回 `not configured` 属预期，表示需先完成「应用凭据 + 授权」，不表示 CLI 未装好。

**首次可用 API 调用前的必做步骤（需浏览器/组织管理员配合）**

- `lark-cli config init` 或 `lark-cli config init --new`：创建/绑定开放平台应用、写入本地配置。
- `lark-cli auth login --recommend`（或按域筛选）：OAuth 授权；完成后再次 `lark-cli auth status` 应显示已授权身份与范围。

### 13.2 官方能力分层（实现时的默认选型顺序）

1. **快捷命令（`+` 前缀）**：适合人机共用、带默认参数与高可读输出（如 `lark-cli calendar +agenda`）。
2. **API 命令**：与开放平台元数据同步的精调命令，形如 `lark-cli <服务> <资源> <方法> --params '{...}'`。
3. **原始调用**：`lark-cli api GET|POST|PUT|DELETE|PATCH <path> [--params] [--data]`，覆盖 2500+ 开放接口，适合底座未封装的端点或临时尝试。
4. **Schema 自省**：`lark-cli schema` / `lark-cli schema <service.resource.method>` 用于确认参数、鉴权与返回结构（实现抽取、写回前的首选核对方式）。

输出格式可统一为 `--format json|ndjson|table|csv|pretty`，长列表可配合 `--page-all` 做分页拉取。

### 13.3 需求章节与 lark-cli / Agent Skills 的对应关系

| 需求点（见上文章节） | 主要覆盖的 Agent Skill | CLI 服务域（示例） | 说明 |
| --- | --- | --- | --- |
| §7.1 文档、Wiki、妙记/会议、IM、任务、日历、多维表格 | `lark-doc`、`lark-wiki`、`lark-drive`、`lark-vc`、`lark-minutes`、`lark-im`、`lark-task`、`lark-calendar`、`lark-base` | `docs` / `drive` / `wiki` / `vc` / 会议与妙记相关域 / `im` / `task` / `calendar` / `base` | 飞书「多维表格」在开放平台侧多对应 **Base** 能力，与 `lark-base` 对齐；广义的 Drive/Wiki/文档检索与 `lark-drive` 配合。 |
| §7.2 会议/日程/文档等事件驱动触发 | `lark-event` | 事件长连接/订阅 + 业务回调处理 | 实时订阅走 `lark-event`；**定时/外部 cron** 仍由 Agent 侧调度器实现，CLI 提供拉取与写回能力。 |
| §7.3 会前卡、会后摘要、总表、周报类产物 | 同上，另可加 `lark-workflow-meeting-summary`、`lark-workflow-standup-report` | 各域 `+` 快捷与 API 命令组合 | 工作流型 Skill 偏「模板化报告」；与 §7.3 的定制抽取之间需做一层产品化封装。 |
| §7.4 任务创建/更新/对账 | `lark-task` | `task` | 与「团队待办中枢」主线的直接支撑域。 |
| §7.5 卡片内确认、认领等 | 以 `lark-im` 为主，底层多为消息与交互卡片相关开放接口 | `im` + `lark-cli api` 对未封装端点 | 需应用具备发送消息、更新卡片/交互等相应权限与事件处理。 |
| §7.6 观测与日志 | 应用层 | — | `lark-cli` 不替代集中日志/追踪；在开发联调阶段可用其结构化输出接入管道。 |

**身份模式**：`--as user|bot|auto` 用于区分用户身份与机器人身份，与 §8「区分应用身份与用户身份」一致，实施时应在编排层显式选择并审计。

### 13.4 与 §12 实施主线的落点

| 主线 | 能力底座落点（CLI / Skill） |
| --- | --- |
| D 待办与对账 | `lark-task`、`lark-base`（总表/多维表）、`lark-contact`（负责人解析） |
| B 会议与项目链路 | `lark-calendar`、`lark-vc`、`lark-minutes`、`lark-doc`/`lark-wiki`/`lark-drive`（材料检索）、`lark-im`（触达） |
| A 周期总结与洞察 | `lark-im`、`lark-doc`、工作流 Skill；数据汇总侧 `lark-base`/`lark-sheets` 视场景选用 |
| C CLI 知识推送 | 已安装的全局 `lark-cli` + 各域 Skill；与定时/本地触发脚本对接 |

### 13.5 使用与风险提示（与官方 README 一致）

- CLI 在已授权下可对租户内数据执行读/写，**默认安全策略**（注入防护、输出脱敏、凭据进系统凭据管理器）建议保持，勿随意放宽。
- 生产环境需结合开放平台应用的最小权限、事件订阅的幂等与重放防护（对应 §7.6、§8）。

### 13.6 与开放平台的扩展查阅

- 对某一接口不确定路径或字段时：优先 `lark-cli schema`；全量探索可用 `lark-openapi-explorer` Skill 对照官方文档。
- 本需求涉及卡片、机器人、长连接时，以开放平台当前版本文档与租户开通的功能为准，实施前做一次 **scope 与可用接口清单** 核对。
