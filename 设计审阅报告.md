# 办公场景驱动的智能知识助手 — 设计审阅报告

> 状态：历史审阅记录。本文档包含早期设计阶段的问题判断，其中部分内容已被后续文档和实现修正，例如 Trigger.dev 选型、对象模型压缩和 OpenClaw 主体化方案。当前整改基线以 `FeishuAgent MVP 审阅整改计划.md` 与 `FeishuAgent 阶段任务规划.md` 为准。

## 1. 审阅目的与范围

### 1.1 审阅目的
本报告对项目当前全部设计产出进行系统性审阅，目标有三：
- 识别文档之间的冲突、不一致与不完善之处。
- 给出可落地的优化与简化建议，确保设计规模与比赛交付周期匹配。
- 逐项检查设计是否满足赛题三项交付物要求，标记缺口并提出补齐方案。

### 1.2 审阅范围
本次审阅覆盖以下四份文档：

| 文档 | 定位 |
|---|---|
| `FeishuProject.md` | 赛题原文 |
| `FeishuAPI.md` | 需求概要说明书（含 lark-cli 能力映射） |
| `架构设计说明书.md` | 总体架构设计（6 层 + 2 横切） |
| `对象模型与模块设计说明.md` | 对象模型、数据库逻辑设计与模块拆分 |

---

## 2. 冲突与不一致问题

### 2.1 工作流编排引擎选型冲突

**严重程度**：高

**问题描述**

`架构设计说明书` §8.3 的技术选型结论为：

> 当前阶段推荐 BullMQ + Redis 或 Trigger.dev

§8.7 最终结论中确定为 `BullMQ + Redis`。

而 `对象模型与模块设计说明` §9.1 及 §11 则以 Trigger.dev 为既定技术主线：

> 不改变 Trigger.dev 与 PostgreSQL + pgvector 的技术主线

两份文档对编排引擎的最终选型互相矛盾。

**影响分析**

编排引擎是系统的调度骨架，直接影响工作流定义方式、重试与补偿策略、任务可观测性和部署拓扑。若团队成员按不同文档实施，会在项目早期引发架构撕裂。

**修正建议**

统一为 **Trigger.dev**。理由：
- Trigger.dev 自带任务调度、重试、补偿、Dashboard 和日志可视化，与本项目"工作流编排层"职责高度匹配。
- TypeScript 原生，与项目既定语言生态一致。
- 相比 BullMQ 需要自建 Worker、Dashboard 和补偿逻辑，Trigger.dev 的开箱即用程度更高，更适合比赛周期。
- 需修订 `架构设计说明书` §8.3 和 §8.7。

---

### 2.2 KnowledgeArtifact 关联字段与关系表重复

**严重程度**：中

**问题描述**

`对象模型与模块设计说明` §5.3 在 KnowledgeArtifact 的核心属性中列出了三个关联数组字段：
- `sourceAssetIds`
- `sourceWorkItemIds`
- `pushRecordIds`

而 §5.7 同时定义了 `artifact_work_item_refs` 和 `artifact_asset_refs` 两张独立关系表，§5.8 的 `push_records` 也通过 `artifactId` 反向关联 KnowledgeArtifact。

数组字段与关系表的职责完全重叠。

**影响分析**

若同时保留两套关联机制，则每次更新关联关系都需要同步维护数组字段和关系表，极易出现不一致。在归并、删除、批量更新等操作中尤其危险。

**修正建议**

删除 §5.3 中的 `sourceAssetIds`、`sourceWorkItemIds`、`pushRecordIds` 三个数组字段，统一由关系表承载。需要在查询时快速获取关联 ID 的场景，通过 SQL JOIN 或必要时通过物化视图/缓存实现。

---

### 2.3 SourceReference 三处定义重叠

**严重程度**：中

**问题描述**

来源引用关系在三个位置被独立定义：
1. `对象模型与模块设计说明` §7.2：定义了通用 `SourceReference` 对象，`targetType` 可为 `work_item` 或 `knowledge_artifact`。
2. §5.7：定义了 `artifact_asset_refs` 表，专用于 KnowledgeArtifact 与 KnowledgeAsset 的关联。
3. §4.8 / §8.1：在事项中心分区列出 `source_references` 表。

三者职责交叉。`SourceReference` 是通用多态关联表，`artifact_asset_refs` 是专用关联表，且二者字段结构高度相似（都有 `relationType`、`excerpt` 等字段）。

**影响分析**

开发者在实现"查找某个 KnowledgeArtifact 的来源资产"时，会不确定应查 `source_references` 还是 `artifact_asset_refs`。

**修正建议**

保留 `source_references` 作为统一的多态引用表，通过 `target_type` 字段区分引用目标是 `work_item` 还是 `knowledge_artifact`，删除 `artifact_asset_refs` 和 `artifact_work_item_refs`。统一表结构：

```
source_references
├── id
├── target_type        (work_item / knowledge_artifact)
├── target_id
├── asset_id           (指向 knowledge_assets)
├── relation_type      (derived_from / evidence_for / related_to / primary_basis / included_item / risk_source / source / context)
├── excerpt
├── confidence_score
├── created_at
```

一张表覆盖所有来源引用需求。

---

### 2.4 架构图数据流与文字描述不一致

**严重程度**：低

**问题描述**

`架构设计说明书` §4 的总体逻辑架构 mermaid 图中画了 `domain --> touchpoint` 的直连箭头，暗示领域智能层可直接输出到触点层。但全文的分层描述中，领域层的输出始终需要经过编排层调度和集成执行层写回后，才到达触点层。领域层本身"不直接操作飞书 API""不承接最终触达与交互"（§5.4）。

**修正建议**

删除架构图中的 `domain --> touchpoint` 箭头。正确的数据回路应为：`domain --> workflow --> integration --> touchpoint`，或 `domain --> workflow --> touchpoint`（对于纯通知场景）。

---

### 2.5 邮件来源缺少数据流落地

**严重程度**：中

**问题描述**

`对象模型与模块设计说明` §2.3 明确提出"把赛题原文中的邮件纳入知识来源建模范围"。`架构设计说明书` §5.2 也将"邮件同步/邮件事件"列为触发源。

但六条关键数据流（§7.1–§7.6）中没有任何一条涵盖邮件的采集与处理路径。邮件只停留在字段枚举级别（`originChannel` 含 `mail`），缺少端到端的流程说明。

**影响分析**

开发时会不清楚邮件事件如何触发、邮件内容如何进入知识资产、邮件中的待办如何沉淀为 WorkItem。

**修正建议**

不必新增独立数据流。在"数据流一：事项入库与归并链路"的分层落点说明中，显式补充邮件路径：
- 触发与接入层：接收邮件同步事件（如 `lark-event` 的邮件到达事件，或定时轮询 `lark-mail`）。
- 集成执行层：通过 `lark-mail` skill 拉取邮件正文、附件摘要和邮件线索 ID。
- 领域智能层：从邮件正文中抽取事项（与会议纪要抽取使用相同的 WorkItem 抽取 Prompt）。
- 知识与状态层：将邮件作为 KnowledgeAsset（assetType = `mail`）入库，将抽取的事项做归并。

---

### 2.6 tenantKey 使用范围不一致

**严重程度**：低

**问题描述**

`work_items`、`knowledge_artifacts`、`trigger_policies` 三张主表包含 `tenantKey` 字段，但所有子表、关系表和审计表均未携带。

**影响分析**

对于比赛 Demo 项目，多租户支持本身是不必要的。若保留但不完整，反而增加开发时的心智负担。

**修正建议**

比赛阶段直接移除所有表的 `tenantKey`，按单租户设计。所有数据天然属于同一租户。若后续需要多租户，可通过 PostgreSQL RLS（Row Level Security）统一加回，不需要在每张表手工加字段。

---

### 2.7 WorkItem 的 originChannel/originContextId 与 source_references 语义边界模糊

**严重程度**：低

**问题描述**

`WorkItem` 主表有 `originChannel` + `originContextId` 记录来源，同时 `source_references` 表也用于记录 WorkItem 与来源资产之间的引用关系。两者语义相近，文档中未做明确区分。

**修正建议**

在文档中补充说明：
- `originChannel` + `originContextId`：仅记录事项**首次被识别**的上下文（如首次从哪场会议中被抽取），用于快速回溯和去重。
- `source_references`：记录事项**所有**相关知识资产引用，随着事项被多次检测、归并和更新而持续增长。

两者不互相替代，有明确的职责分工。

---

## 3. 优化与简化建议

### 3.1 数据库表从 22+ 张压缩到 12 张

**现状**

当前对象模型定义了约 22 张数据库表。对比赛 Demo 而言，这个规模会严重拖慢数据库迁移、ORM 建模和联调速度。

**压缩方案**

| 压缩项 | 原设计 | 建议 | 理由 |
|---|---|---|---|
| WorkItem 子类型表 | 4 张子表（todos / decisions / risks / blockers） | 合并为 `work_items` 主表的 `metadata JSONB` 字段 | 每种 itemType 的差异字段放入 JSON，PostgreSQL JSONB 索引可满足查询需求；赛题要求快速交付，不要求极致关系型范式 |
| 审计表 | `work_item_state_history` + `work_item_change_logs` 2 张 | 合为 1 张 `work_item_audit_log` | 用 `change_type` 字段区分状态迁移与字段变更，减少表间跳转 |
| 引用关系表 | `source_references` + `artifact_asset_refs` + `artifact_work_item_refs` 3 张 | 合为 1 张 `source_references` | 如 §2.3 所述，统一多态引用表 |
| 评测表 | `evaluation_cases` + `evaluation_runs` + `metric_snapshots` 3 张 | 合为 1 张 `evaluation_records` | 评测体系在比赛阶段尚属早期，单表足以承载 |

**压缩后推荐表清单（12 张）**

```
事项中心（4 张）
├── work_items              事项主表（含 metadata JSONB）
├── work_item_participants  人员关系
├── task_bindings           任务/总表绑定
└── work_item_audit_log     审计日志

知识中心（3 张）
├── knowledge_assets        原始知识资产
├── knowledge_artifacts     知识产物
└── source_references       统一来源引用

触发与交互（3 张）
├── trigger_policies        触发策略
├── human_review_tasks      人工确认任务
└── push_records            分发记录

过程与评测（2 张）
├── execution_records       工作流执行记录
└── evaluation_records      评测记录
```

**预期收益**：表数量减少 45%，ORM 模型减少近一半，迁移脚本和测试 Fixture 工作量同步下降。

---

### 3.2 模块从 10 个合并到 6 个

**现状**

当前模块拆分为 10 个：trigger / workflow / knowledge / workitem / artifact / review / policy / integration / evaluation / observability。

对比赛项目而言，模块间接口定义、依赖注入和测试隔离的成本会远超收益。

**合并方案**

| 合并后模块 | 吸收的原模块 | 职责 |
|---|---|---|
| `trigger` | trigger-module | 事件接入、定时调度、CLI 触发 |
| `workflow` | workflow-module + policy-module + review-module | 工作流编排、触发策略执行、人工确认节点 |
| `domain` | knowledge-module + workitem-module + artifact-module | 知识资产管理、事项归并/去重/状态迁移、知识产物生成 |
| `integration` | integration-module | lark-cli / OpenAPI 适配层 |
| `evaluation` | evaluation-module + observability-module | 评测执行、日志、链路追踪、指标采集 |
| `touchpoint` | （新增，从各模块中抽离触达逻辑） | 卡片渲染、消息发送、CLI 输出格式化 |

**预期收益**：模块数量减少 40%，模块间接口数量大幅下降，单模块职责更饱满。

---

### 3.3 TriggerPolicy 简化

**现状**

`TriggerPolicy` 设计了 `conditionExpression`、`expressionVersion`、`inputSchemaHint` 三个字段，暗示需要构建运行时可编辑的 DSL 解释器。

**建议**

比赛阶段采用 **代码配置驱动** 策略：
- 在 TypeScript 代码中定义策略枚举和条件判断函数。
- `trigger_policies` 表仅记录策略的启用状态、执行历史和效果统计。
- 移除 `conditionExpression`、`expressionVersion`、`inputSchemaHint` 三个字段。
- 将 DSL 引擎列入"未来演进"路线图。

**预期收益**：避免在比赛周期内投入解释器开发，将精力集中在核心业务逻辑上。

---

### 3.4 补充 LLM 集成设计

**现状**

四份文档均未提及 LLM 选型、Prompt 工程策略、调用方式和成本控制。领域智能层是项目核心价值所在（负责事项抽取、知识提纯、风险识别），但当前设计中它是最抽象、最缺乏细节的一层。

**建议补充内容**

| 设计维度 | 需要回答的问题 |
|---|---|
| 模型选型 | 使用哪家 LLM？主力模型和降级模型分别是什么？ |
| Prompt 管理 | Prompt 模板是硬编码还是版本化管理？是否支持 A/B 测试？ |
| 结构化输出 | 如何确保 LLM 返回可解析的结构化事项？使用 function calling / JSON mode / 后处理解析？ |
| 调用链路 | 编排层如何向领域层传递上下文？领域层如何返回结构化结果？ |
| Token 预算 | 单次抽取的输入 Token 上限？超长文档如何分片？ |
| 降级策略 | LLM 不可用或超时时的兜底方案是什么？ |

此部分应作为架构设计说明书的新章节（建议为 §8.8 LLM 集成设计）或独立文档。

---

### 3.5 补充卡片交互设计

**现状**

触点层列出了"飞书消息卡片"作为核心载体，也提到了"确认、认领、驳回、补充输入"等交互能力。但没有卡片模板结构、交互按钮布局和回调流程的任何设计。

**建议**

至少为以下三类核心卡片提供结构草案：
1. **会前背景卡片**：展示即将召开会议的历史决策、未结事项、相关文档链接。
2. **会后确认卡片**：展示 AI 抽取的事项列表，支持逐条确认/驳回/修改负责人。
3. **风险预警卡片**：展示超期或阻塞事项，支持认领和标注处理进展。

每类卡片需明确：JSON 结构骨架、交互按钮及其回调路由、回调后的状态更新流程。

---

### 3.6 对齐演进路径与比赛周期

**现状**

`架构设计说明书` §12.2 列出五个演进阶段，但未标注预计工期和里程碑。

**建议**

制定面向比赛交付的 MVP 时间线：

| 阶段 | 范围 | 里程碑 |
|---|---|---|
| P0（核心闭环） | B 入口：会后事项抽取 -> WorkItem 入库 -> 飞书任务创建 -> 确认卡片 | 可演示的端到端闭环 |
| P1（事项中枢） | D 骨架：事项归并去重 + 总表投影 + 状态对账 | 统一推进总表可用 |
| P2（主动输出） | A 输出：周期性洞察卡片 + 管理报告 | 定时推送可用 |
| P3（扩展触达） | C 扩展：CLI 即时知识推送 | CLI Demo 可用 |
| P4（效果验证） | 评测用例执行 + 效果验证报告 | 交付评测报告 |

每个阶段在进入下一阶段前应有可独立演示的产出。

---

## 4. 与赛题交付要求的对齐检查

### 4.1 交付物一：场景定义文档

**赛题要求**：说明你选择的知识场景、目标用户、核心价值，以及为什么通用搜索/问答无法满足。

**当前状态**：基本满足。`FeishuAPI.md` §2–§6 已覆盖场景选择（D 为主线、B 为入口、A/C 为输出面）、目标用户（一线协作团队、管理者、研发）、核心价值（统一事项视图、主动推送、执行闭环）和业务痛点。

**缺口**：赛题明确要求回答"为什么通用搜索/问答无法满足"，当前文档缺少这一对比论证。

**补齐建议**：在 `FeishuAPI.md` §5 之后补充一节"与通用搜索/问答的对比"，从以下维度论证差异：

| 维度 | 通用搜索/问答 | 本系统 |
|---|---|---|
| 触发模式 | 被动，用户必须主动发起搜索 | 主动，系统根据事件和策略自动触发 |
| 知识形态 | 返回原始文档片段 | 返回经过提纯的结构化事项和知识产物 |
| 状态追踪 | 无，每次搜索独立 | 持续追踪事项状态，支持对账和预警 |
| 执行闭环 | 无，搜索结果需人工搬运 | 自动创建任务、绑定负责人、同步状态 |
| 上下文感知 | 无，不了解用户当前场景 | 基于会议、文档、时间窗口的场景化推送 |

---

### 4.2 交付物二：可运行的 Demo

**赛题要求**：基于 OpenClaw/CLI 实现的主动知识服务，至少包含一种主动触发方式（定时、事件驱动、阈值触发）。

**当前状态**：架构设计就绪，技术底座映射清晰。

**缺口**：
1. 缺少 MVP 功能清单——哪些功能进第一版 Demo、哪些延后。
2. 缺少代码目录结构设计。
3. 缺少 LLM 集成方案（见 §3.4）。

**补齐建议**：
- 输出一份"MVP 交付范围与优先级"文档。
- 补充代码目录结构设计（建议按 `src/trigger`、`src/workflow`、`src/domain`、`src/integration`、`src/evaluation`、`src/touchpoint` 组织）。
- 补充 LLM 集成设计章节。

---

### 4.3 交付物三：效果验证报告

**赛题要求**：自证知识产物的准确性（人工评估或引用来源）、用户接受度（如卡片点击率、任务完成率），以及相比传统方式的效率提升。

**当前状态**：框架存在但骨架化。`FeishuAPI.md` §11 定义了四类成功指标，`对象模型与模块设计说明` §7.7 定义了 EvaluationCase / EvaluationRun / MetricSnapshot 对象。

**缺口**：
1. 缺少具体评测用例——需要至少 5–10 条可执行的用例。
2. 缺少评分标准——准确率、召回率如何计算。
3. 缺少基线对比方法——"传统方式"如何量化。

**补齐建议**：

设计以下评测用例（示例）：

| 编号 | 评测维度 | 用例描述 | 评分方式 |
|---|---|---|---|
| E1 | 事项抽取准确率 | 从 3 场真实会议纪要中抽取 Action Items，与人工标注对比 | Precision / Recall / F1 |
| E2 | 负责人推断准确率 | 对抽取的事项推断负责人，与人工标注对比 | 准确率 |
| E3 | 事项归并准确率 | 将来自多来源的相同事项归并，检查是否正确归并/误合并 | 归并准确率 / 误合并率 |
| E4 | 卡片接受度 | 推送会后确认卡片，统计确认率、驳回率、修改率 | 确认率 |
| E5 | 效率提升 | 对比人工整理会后 Action Items 的耗时 vs 系统自动整理的耗时 | 时间下降比 |
| E6 | 来源可追溯性 | 检查每条事项是否附带正确的来源链接 | 来源覆盖率 |

---

### 4.4 赛题"三个挑战"覆盖检查

#### 挑战一：重新定义"知识获取"

> 如何将万字长文或零碎的聊天记录，提炼成当前场景下最需要的"高密度知识"？

**当前覆盖**：WorkItem + KnowledgeArtifact 双核心模型从架构层面回答了"从原始数据到高密度知识"的转化路径。领域智能层负责抽取、归并、补齐和提纯。

**缺口**：
- "知识密度"缺乏量化定义——什么是"高密度"？如何衡量密度？
- 提纯策略的具体设计缺失——LLM 如何从万字长文中筛选出当前场景最需要的信息？是否有分级提纯（摘要 -> 结构化事项 -> 行动卡片）？

**建议**：在领域智能层设计中补充"知识提纯管道"的分层设计：
1. **第一层：内容切片与预过滤**——基于时间窗口、参与人、关键词做粗筛。
2. **第二层：结构化抽取**——LLM 从粗筛内容中抽取事项、决策、风险。
3. **第三层：场景化排序与聚合**——根据当前触发上下文（如即将召开的会议主题）对结果做相关性排序和聚合。

#### 挑战二：构建场景化知识应用

> 发挥创造力，设计一个能在恰当时机、以恰当形态主动服务用户的知识产物。

**当前覆盖**：A/B/C/D 四方向覆盖全面，触发方式多样（定时 / 事件 / 阈值 / 人工）。

**缺口**：缺少具体的交互原型。卡片长什么样、CLI 输出什么格式、报告文档的结构模板——这些直接决定用户感知，但尚未设计。

**建议**：见 §3.5 卡片交互设计建议。

#### 挑战三：证明应用的效果与价值

> 设计一套评测用例和指标，证明你的产物不仅内容准确，而且真的提升了团队的信息流转效率。

**当前覆盖**：有指标框架（4 类指标）和评测对象模型（EvaluationCase / EvaluationRun / MetricSnapshot）。

**缺口**：评测用例和执行方案骨架化，不足以支撑最终的效果验证报告。

**建议**：见 §4.3 补齐建议。

---

## 5. 压缩后推荐数据库设计

### 5.1 表清单（12 张）

#### 事项中心

**work_items**
```
id                     UUID PRIMARY KEY
title                  TEXT NOT NULL
item_type              TEXT NOT NULL  -- todo / decision / risk / blocker
status                 TEXT NOT NULL  -- new / pending_review / active / blocked / done / closed
priority               TEXT          -- low / medium / high / critical
owner_user_id          TEXT
owner_source           TEXT          -- manual / inferred / inherited
due_at                 TIMESTAMPTZ
confidence_score       REAL
need_human_confirm     BOOLEAN DEFAULT false
origin_channel         TEXT          -- meeting / minutes / doc / wiki / im / task / mail
origin_context_id      TEXT
current_primary_binding_id  UUID
current_review_task_id      UUID
dedupe_key             TEXT
metadata               JSONB         -- 子类型差异字段（替代 4 张子表）
first_detected_at      TIMESTAMPTZ
last_detected_at       TIMESTAMPTZ
last_synced_at         TIMESTAMPTZ
last_touched_by_workflow TEXT
created_at             TIMESTAMPTZ DEFAULT now()
updated_at             TIMESTAMPTZ DEFAULT now()
deleted_at             TIMESTAMPTZ
```

**work_item_participants**
```
id              UUID PRIMARY KEY
work_item_id    UUID NOT NULL REFERENCES work_items(id)
user_id         TEXT NOT NULL
role            TEXT NOT NULL  -- owner / follower / collaborator / watcher
source          TEXT           -- manual / inferred / imported
created_at      TIMESTAMPTZ DEFAULT now()
```

**task_bindings**
```
id              UUID PRIMARY KEY
work_item_id    UUID NOT NULL REFERENCES work_items(id)
binding_type    TEXT NOT NULL  -- feishu_task / bitable_record
external_id     TEXT NOT NULL
is_primary      BOOLEAN DEFAULT false
binding_role    TEXT           -- execution / tracking / projection
sync_status     TEXT           -- pending / synced / conflict / failed
last_sync_at    TIMESTAMPTZ
last_sync_error TEXT
created_at      TIMESTAMPTZ DEFAULT now()
updated_at      TIMESTAMPTZ DEFAULT now()
```

**work_item_audit_log**
```
id                  UUID PRIMARY KEY
work_item_id        UUID NOT NULL REFERENCES work_items(id)
change_type         TEXT NOT NULL  -- status_change / field_update / create / merge / close
from_value          TEXT
to_value            TEXT
field_name          TEXT
reason              TEXT
changed_by_type     TEXT           -- workflow / human / sync / policy
changed_by_id       TEXT
execution_record_id UUID
created_at          TIMESTAMPTZ DEFAULT now()
```

#### 知识中心

**knowledge_assets**
```
id              UUID PRIMARY KEY
asset_type      TEXT NOT NULL  -- doc / docx / wiki / minutes / meeting_notes / im_message / task_snapshot / mail
source_id       TEXT NOT NULL
title           TEXT
content_text    TEXT
content_chunk_ref TEXT
vector_ref      TEXT
owner_user_id   TEXT
created_at      TIMESTAMPTZ DEFAULT now()
updated_at      TIMESTAMPTZ DEFAULT now()
```

**knowledge_artifacts**
```
id                      UUID PRIMARY KEY
artifact_type           TEXT NOT NULL  -- pre_meeting_brief / meeting_summary / risk_report / weekly_insight / cli_hint / task_digest
title                   TEXT
summary                 TEXT
content_payload         JSONB
canonical_render_format TEXT          -- card / doc / table / cli_text
template_version        TEXT
audience_type           TEXT          -- user / group / department / cli_local
confidence_score        REAL
traceability_level      TEXT
status                  TEXT          -- draft / ready / published / expired / archived
is_evaluated            BOOLEAN DEFAULT false
created_by_workflow     TEXT
generated_at            TIMESTAMPTZ
expires_at              TIMESTAMPTZ
created_at              TIMESTAMPTZ DEFAULT now()
updated_at              TIMESTAMPTZ DEFAULT now()
```

**source_references**
```
id                UUID PRIMARY KEY
target_type       TEXT NOT NULL  -- work_item / knowledge_artifact
target_id         UUID NOT NULL
asset_id          UUID REFERENCES knowledge_assets(id)
relation_type     TEXT NOT NULL  -- derived_from / evidence_for / related_to / primary_basis / included_item / risk_source
excerpt           TEXT
confidence_score  REAL
created_at        TIMESTAMPTZ DEFAULT now()
```

#### 触发与交互

**trigger_policies**
```
id                UUID PRIMARY KEY
policy_name       TEXT NOT NULL
policy_scope      TEXT           -- B / D / A / C
trigger_type      TEXT NOT NULL  -- scheduled / event / threshold / manual
trigger_source    TEXT           -- meeting_end / meeting_start / doc_edit / task_change / mail_sync / cli
cooldown_window   INTERVAL
artifact_type     TEXT
delivery_channel  TEXT           -- card / im / doc / base / cli
target_selector   TEXT
review_requirement TEXT          -- none / high_risk_only / always
enabled           BOOLEAN DEFAULT true
last_triggered_at TIMESTAMPTZ
success_count     INTEGER DEFAULT 0
failure_count     INTEGER DEFAULT 0
acceptance_rate   REAL
created_at        TIMESTAMPTZ DEFAULT now()
updated_at        TIMESTAMPTZ DEFAULT now()
```

**human_review_tasks**
```
id                UUID PRIMARY KEY
target_type       TEXT NOT NULL  -- work_item / knowledge_artifact
target_id         UUID NOT NULL
review_reason     TEXT
review_status     TEXT NOT NULL  -- pending / approved / rejected / revised
assigned_reviewer TEXT
reviewed_at       TIMESTAMPTZ
created_at        TIMESTAMPTZ DEFAULT now()
```

**push_records**
```
id                  UUID PRIMARY KEY
artifact_id         UUID REFERENCES knowledge_artifacts(id)
channel_type        TEXT NOT NULL  -- card / im / doc / base / cli
target_type         TEXT           -- user / group / department / cli_local
target_id           TEXT
render_version      TEXT
rendered_payload    JSONB
external_message_id TEXT
delivery_status     TEXT           -- pending / sent / failed / acknowledged
clicked             BOOLEAN DEFAULT false
acknowledged_at     TIMESTAMPTZ
sent_at             TIMESTAMPTZ
created_at          TIMESTAMPTZ DEFAULT now()
```

#### 过程与评测

**execution_records**
```
id                  UUID PRIMARY KEY
workflow_name       TEXT NOT NULL
trigger_type        TEXT
trigger_context_id  TEXT
status              TEXT NOT NULL  -- running / succeeded / failed / compensating
started_at          TIMESTAMPTZ
finished_at         TIMESTAMPTZ
error_code          TEXT
error_message       TEXT
created_at          TIMESTAMPTZ DEFAULT now()
```

**evaluation_records**
```
id                UUID PRIMARY KEY
eval_type         TEXT NOT NULL  -- case_definition / run_result / metric_snapshot
case_name         TEXT
scenario_type     TEXT
input_data        JSONB
expected_output   JSONB
actual_output     JSONB
score             REAL
metric_window     TSTZRANGE
metric_data       JSONB
created_at        TIMESTAMPTZ DEFAULT now()
```

### 5.2 推荐索引

```sql
-- work_items 高频查询索引
CREATE INDEX idx_work_items_status ON work_items(status);
CREATE INDEX idx_work_items_owner_status ON work_items(owner_user_id, status);
CREATE INDEX idx_work_items_type_status ON work_items(item_type, status);
CREATE INDEX idx_work_items_due_at ON work_items(due_at);
CREATE INDEX idx_work_items_confirm ON work_items(need_human_confirm, status);
CREATE INDEX idx_work_items_origin ON work_items(origin_channel, origin_context_id);
CREATE INDEX idx_work_items_dedupe ON work_items(dedupe_key);

-- task_bindings 唯一约束
CREATE UNIQUE INDEX idx_task_bindings_primary
    ON task_bindings(work_item_id, binding_type) WHERE is_primary = true;

-- source_references 查询索引
CREATE INDEX idx_source_refs_target ON source_references(target_type, target_id);
CREATE INDEX idx_source_refs_asset ON source_references(asset_id);

-- push_records 查询索引
CREATE INDEX idx_push_records_artifact ON push_records(artifact_id);
CREATE INDEX idx_push_records_status ON push_records(delivery_status);
```

---

## 6. 压缩后推荐模块清单

```
src/
├── trigger/            事件接入、定时调度、CLI 触发入口
├── workflow/           Trigger.dev 工作流定义、触发策略执行、人工确认节点
├── domain/             知识资产管理、事项归并/去重/状态迁移、知识产物生成、LLM 调用
├── integration/        lark-cli / OpenAPI 适配层（会议、日历、任务、文档、消息、Base、邮件）
├── touchpoint/         卡片渲染、消息发送、文档生成、CLI 输出格式化
├── evaluation/         评测执行、日志采集、链路追踪、指标沉淀
└── shared/             通用类型定义、数据库客户端、配置管理、工具函数
```

各模块职责边界：

| 模块 | 输入 | 输出 | 不应承担 |
|---|---|---|---|
| trigger | 飞书事件、定时器、CLI 命令 | 标准化事件上下文 | 业务逻辑、LLM 调用 |
| workflow | 标准化事件、人工确认结果 | 编排调用序列、流程状态 | 直接持有数据主存 |
| domain | 原始内容、编排上下文、已有事项 | 结构化事项、知识产物、置信度 | 飞书 API 调用、消息发送 |
| integration | 编排层执行请求 | 飞书资源读写结果 | 业务判断、流程编排 |
| touchpoint | 产物数据、渲染模板 | 可发送的卡片/消息/文档/CLI 文本 | 业务推理、状态管理 |
| evaluation | 执行日志、用户反馈 | 评测报告、指标快照 | 业务流程 |

---

## 7. 审阅结论与下一步行动

### 7.1 结论

当前设计在架构分层、对象模型抽象和赛题方向覆盖上表现出较高的完成度。6 层 + 2 横切的架构设计思路清晰，WorkItem + KnowledgeArtifact 双核心模型抓住了"事项闭环 + 知识输出"的核心矛盾，四方向（A/B/C/D）的映射关系也与赛题要求对齐。

主要问题集中在三个方面：
1. **文档间存在 7 处不一致**，其中编排引擎选型冲突和关联模型重复需优先修正。
2. **设计规模偏重**，22+ 张表和 10 个模块对比赛周期构成交付风险，建议压缩至 12 张表和 6 个模块。
3. **关键能力设计缺失**，LLM 集成、卡片交互和评测用例三块内容需要尽快补齐。

### 7.2 下一步行动项（按优先级排列）

| 优先级 | 行动项 | 产出物 |
|---|---|---|
| P0 | 统一编排引擎选型为 Trigger.dev，修订架构说明书 §8.3 和 §8.7 | 架构说明书修订版 |
| P0 | 解决 SourceReference 三处重叠，统一为单表设计 | 对象模型修订版 |
| P0 | 删除 KnowledgeArtifact 的冗余数组字段 | 对象模型修订版 |
| P0 | 压缩数据库表至 12 张，输出最终 DDL 脚本 | `schema.sql` |
| P1 | 补充 LLM 集成设计（模型选型、Prompt 管理、结构化输出） | 架构说明书新章节或独立文档 |
| P1 | 设计 3 类核心卡片的交互结构和回调流程 | 卡片设计文档 |
| P1 | 制定 MVP 交付范围与分阶段时间线 | MVP 计划文档 |
| P2 | 补充"与通用搜索/问答对比"论证 | FeishuAPI.md 修订版 |
| P2 | 设计 5–10 条具体评测用例和评分标准 | 评测方案文档 |
| P2 | 补充邮件数据流路径说明 | 架构说明书修订版 |
| P3 | 修正架构图 `domain --> touchpoint` 错误箭头 | 架构说明书修订版 |
| P3 | 移除 tenantKey，统一为单租户设计 | 数据库设计修订版 |
| P3 | 明确 originChannel 与 source_references 的语义边界 | 对象模型修订版 |
