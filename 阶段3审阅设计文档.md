# 阶段 3 审阅设计文档

## 1. 文档目的

本文记录本次文档收敛操作、当前阶段 3 的代码与文档事实、相对 `FeishuProject.md` 最终要求的缺口，以及下一步实现目标。

阶段 3 的核心目标仍是从“会议入口”扩展到“多源事项中枢”：WorkItem 可以来自 meeting / doc / wiki / im / task / mail，能够保留多来源证据、完成跨源归并，并在低置信或高影响场景下进入人工确认。

## 2. 本次文档收敛操作

### 2.1 新增归档目录

已创建：

```text
docs/archive/
```

该目录用于保存历史审阅、过程验证和周报类材料。归档文档可用于追溯项目演进，但不再作为当前设计或交付判断的优先入口。

### 2.2 已归档文档

| 文档 | 归档原因 |
| --- | --- |
| `docs/archive/设计审阅报告.md` | 早期设计审阅记录，部分问题已被后续文档和代码修正。 |
| `docs/archive/阶段2设计审阅报告.md` | 阶段 2 / 方向 D 事件中枢审阅记录，结论已沉淀到阶段 3 任务和 Hub 设计中。 |
| `docs/archive/OpenClaw集成验证记录.md` | 本地过程验证材料，虽然已脱敏，但不应作为当前架构事实入口。 |
| `docs/archive/第三期周报.md` | 周报类过程材料，阶段 3 差距表已在本文重新收敛。 |

### 2.3 当前主线文档入口

根目录保留以下主线文档：

| 文档 | 当前定位 |
| --- | --- |
| `FeishuProject.md` | 赛题原文和最终交付要求。 |
| `README.md` | 项目现状、运行提示和文档导航。 |
| `FeishuAgent 阶段任务规划.md` | 阶段划分、阶段 3 任务和后续验收标准。 |
| `概要设计说明书+FeishuAPI.md` | 场景定义、需求范围、飞书能力映射和评测方案。 |
| `架构设计说明书.md` | 最终系统架构设计和当前实现状态说明。 |
| `对象模型与模块设计说明.md` | WorkItem / KnowledgeArtifact / SourceReference 等对象模型。 |
| `模块接口文档.md` | 目标接口和后续整改方向。 |
| `OpenClaw主体化方案.md` | OpenClaw Agent 主体与 `agent-tools` 边界。 |
| `WorkItem Hub 归属边界条件设计.md` | 多 Hub 归属、投影、触达和 OpenClaw 判定边界。 |
| `FeishuAgent MVP 审阅整改计划.md` | MVP 审阅与整改基线。 |
| `效果验证报告.md` | 当前仍是样例 / 历史快照，最终交付前需用真实样本重算。 |

## 3. 当前代码事实

本轮重点核对了 `src/trigger`、`src/workflow`、`src/application`、`src/domain`、`schema.sql` 和 `README.md` 的阶段 3 相关实现。

### 3.1 已具备

| 能力 | 当前事实 |
| --- | --- |
| 事件分发注册表 | `src/trigger/dispatcher.ts` 已使用 `eventHandlers` 注册会议、卡片回调、IM、doc/wiki/task/mail 等事件键。 |
| IM 入站分流 | `src/trigger/feishu-ingestion-adapter.ts` 已区分 `/help`、Hub 指令、@机器人问询、关键词 source ingestion 和 ignore。 |
| IM 关键词摄入 | 普通群消息命中待办 / 任务 / 风险 / 阻塞等关键词时，可进入 `source-ingestion`。 |
| 多源摄入工作流 | `src/workflow/source-ingestion.ts` 已通过 `ingestSourceItemsFlow` 执行知识资产入库、事项抽取、归并、Hub 归属和 Base 投影。 |
| SourceReference 基础 | `src/application/source-ingestion-service.ts` 在 `reconcileAndSave` 时传入 `sourceAssetId`、`sourceExcerpt` 和 `mergeAcrossSources`。 |
| Hub 归属服务 | `src/application/hub-assignment-service.ts` 已实现证据包、OpenClaw 判定、确定性兜底、投影写入和审计记录。 |
| Guardrails | `src/domain/hub-assignment.ts` 已实现显式绑定优先、私聊不默认进团队 Hub、文档 @ 不自动扩散等约束。 |
| 多 Hub 数据模型 | `schema.sql` 已包含 `item_hubs`、`hub_members`、`work_item_hub_projections`、`hub_resource_bindings`、`source_context_snapshots`、`hub_assignment_decisions`、`hub_audit_log`。 |
| 卡片回调入口 | `dispatcher` 已接入 `card-callback-service`，不再只是文档目标。 |

### 3.2 部分具备但仍需验证

| 能力 | 当前风险 |
| --- | --- |
| OpenClaw 飞书端自然语言入口 | `agent_question` 路径可调用 `callAgent`，但仍需真实飞书群里验证自动选择工具的稳定性。 |
| Hub 指令与权限 | 已有 `/hub`、`/select-hub`、`/add`、`/list` 等分流，权限、成员生命周期和多 Hub 歧义处理仍需真实数据验证。 |
| Hub 归属自动决策 | 已有 LLM + 兜底 + guardrails，但需要用真实会议 / IM / 文档样本验证误路由率。 |
| Base 多 Hub 投影 | 数据模型和部分服务已具备，仍需确认每个 Hub 的 Base token / table id 和 `bitable_record` 回写一致性。 |
| 会前背景卡片 | 有 `pre-meeting` 工作流和相关 artifact 生成，但日历、文档检索和真实推送未闭环。 |
| 效果验证 | 有评测脚本和样例报告，但缺真实人工标注样本与卡片交互数据。 |

### 3.3 未闭环

| 能力 | 当前状态 |
| --- | --- |
| Docs / Wiki 真实事件订阅 | Docs / Wiki 正文拉取 adapter 已具备，真实 fetch / append / source_pull 已通过；此前本地消费者只订阅 IM/card，未覆盖 doc/wiki 事件键，现已改为配置化订阅和别名映射。 |
| 飞书任务状态对账 | `task_update` 已有事件 payload / 拉取详情两条路径和本地 mock 回归；真实状态回流仍缺低风险真实任务绑定。 |
| 邮件来源 | `mail_received` 已有正文拉取、正文清洗、去重 source id 和事项抽取 payload；真实邮件事件与权限仍待联调。 |
| 文档评论来源 | 文档中已设计，但代码入口仍未形成稳定 adapter。 |
| 真实端到端 Demo | 仍需真实 `meetingId`、`chatId`、负责人 `open_id` 和 Base 权限完成演示。 |
| 最终效果证明 | `效果验证报告.md` 当前不是最终报告。 |

### 3.4 真实联调记录（2026-05-06）

已通过：

- IM 真实事件和机器人回复：`/help`、`/hub`、`/add` 草稿卡片、`/list`、关键词 source-ingestion 分发、闲聊 ignore、`/ask` OpenClaw 路径。
- Doc / Wiki：创建、fetch、append、真实 source_pull 拉取通过。
- 本地回归：`build`、IM loopback、doc-wiki loopback、task update mock、card callback loop。

未完成或跳过：

- 未收到真实 `doc_update` / `wiki_update` 实时事件，原因是当时本地服务订阅范围只有 `im.message.receive_v1` 与 `card.action.trigger`。现已增加 `FEISHU_EVENT_TYPES` / `FEISHU_EXTRA_EVENT_TYPES` 订阅配置，以及 `FEISHU_DOC_EVENT_TYPES`、`FEISHU_WIKI_EVENT_TYPES`、`FEISHU_TASK_EVENT_TYPES`、`FEISHU_MAIL_EVENT_TYPES` adapter 别名配置。
- 任务状态回流真实测试跳过，因为缺少可安全变更状态的真实任务绑定。
- `/add` 当前设计为“先出草稿卡片、用户确认后落库”，用于避免群内误触发直接写入 WorkItem；真实点击确认仍归入卡片人工联调。

### 3.5 相对最终目标的缺口分组

已实现 / 基本通过，无需重复投入：

- D 主线的 WorkItem、SourceReference、KnowledgeAsset、Hub 归属、Base 投影和审计骨架。
- IM 指令、关键词摄入、闲聊忽略、OpenClaw 问询入口和机器人出站。
- Doc / Wiki 正文拉取到 source-ingestion payload 的本地闭环与真实 source_pull fetch。
- Task 状态映射与 WorkItem 回写的 mock 闭环。
- 卡片确认、驳回、修改、认领回调的本地数据库闭环。

尚未实现但不依赖人工资源，可继续编码：

- 文档评论来源 adapter：把评论作者、@对象、评论片段和 doc token 归入证据包。
- 更完整的 source event payload 归一化样例库：覆盖真实飞书官方 EventKey 字段差异。
- 周报 / 风险巡检等主动知识产物的本地 dry-run 验证脚本。
- 最终交付文档压缩模板与真实评测数据导入脚本。

需要人工联调 / 真实资源 / 权限：

- 飞书开放平台为 doc/wiki/task/mail 开通并确认真实 EventKey、scope 和事件订阅。
- 真实 doc/wiki 更新事件触发服务消费，确认字段能映射到 token/url 并进入 source-ingestion。
- 低风险真实任务与 WorkItem 绑定后，验证任务状态变更回流。
- 真实邮件读取权限、测试邮箱或脱敏邮件样本，用于验证 mail_received。
- 真实用户点击 `/add` 草稿卡片和确认 / 驳回 / 修改卡片，验证卡片交互记录、WorkItem 状态、Base 投影和指标。
- 3-5 场真实会议 / 多源样本人工标注，用于最终 Precision / Recall / F1、确认率、任务完成率和人工耗时对比。

## 4. 与最终要求的差距

`FeishuProject.md` 要求三类交付物：场景定义文档、可运行 Demo、效果验证报告。

### 4.1 场景定义文档

当前基本具备。`概要设计说明书+FeishuAPI.md` 已说明 D 主线、B 入口、目标用户、核心价值和“为什么通用搜索/问答无法满足”。后续需要在最终交付前压缩成面向评审的 `场景定义文档.md`，避免评审阅读过多过程文档。

### 4.2 可运行 Demo

当前部分具备。会议结束、IM 关键词、Hub 指令、OpenClaw 问询、source ingestion、WorkItem 入库、Base 投影等路径已具备代码基础，但最终 Demo 还缺：

1. 真实飞书资源联调。
2. Docs / Wiki / Task / Mail 至少补齐 1-2 条非会议来源闭环。
3. 卡片确认 / 驳回 / 修改数据能进入状态和指标。
4. OpenClaw 在飞书端稳定选择 `agent-tools`。

### 4.3 效果验证报告

当前不足。样例报告只能证明报告格式和指标口径，不足以满足赛题“自证价值”。最终必须基于 3-5 场真实会议和至少一批真实或脱敏多源事项样本，补充 Precision / Recall / F1、负责人推断准确率、卡片确认率、任务完成率和人工耗时对比。

## 5. 阶段 3 下一步目标

### P0：把 `pending_pull` 变成真实多源摄入

1. 为 doc/wiki 增加内容拉取 adapter：事件 token -> docs/wiki/drive 拉取正文 -> `source-ingestion`。
2. 为 task 增加状态对账 adapter：task id -> 任务详情 -> WorkItem 状态 / owner / dueAt 回写。
3. 为 mail 增加正文拉取 adapter：mail id / thread id -> 正文摘要 -> 去重 -> `source-ingestion`。
4. 为文档评论增加最小闭环：评论作者、@对象、评论片段和 doc token 进入证据包。

验收：至少 `im + doc/wiki + task` 三类来源可产生或更新 WorkItem，并保留 source reference。

### P0：真实 IM 与 Hub 链路验证

1. 用真实群验证 `/help`、`/hub`、`/add`、`/list`、`/select-hub`。
2. 验证 @机器人自然语言问询是否稳定调用 OpenClaw。
3. 验证普通闲聊不会进入 OpenClaw 或 source ingestion。
4. 验证 IM 出站均由应用机器人发送，Base 权限单独验证。

验收：群内指令、普通消息、@机器人问询三类路径可解释、可审计、无误触发。

### P1：Hub 投影可信化

1. 建立默认 `legacy` Hub 迁移脚本。
2. 补齐 `chat_id -> hub_id`、资源 token -> Hub 的绑定管理。
3. 验证同一 WorkItem 在团队 Hub 和个人 Hub 的投影一致性。
4. 补充成员移除、owner_admin 保底、Hub 解散和审计日志。

验收：Base record 能准确反查 `work_item_id + hub_id`，跨 Hub 投影不混表。

### P1：卡片与人工确认闭环

1. 完整验证确认、驳回、修改、补充遗漏和风险认领回调。
2. 将卡片交互写入 `push_records`、WorkItem 状态和审计日志。
3. 对低置信 Hub 归属生成确认卡片。

验收：用户点击卡片后，数据库状态、Base 投影和指标记录都能同步变化。

### P2：最终交付文档压缩

最终交付前建议从当前主线文档中抽取 4 份评审入口：

```text
docs/final/
  01-场景定义文档.md
  02-架构与实现说明.md
  03-Demo运行说明.md
  04-效果验证报告.md
```

根目录仍保留开发文档，但评审材料只引用 `docs/final/`，避免文档再次膨胀。

## 6. 当前阶段判断

项目方向与 `FeishuProject.md` 仍然一致：以 D 方向团队待办中枢为主线，以 B 方向会议入口作为首个可演示场景，并通过 OpenClaw + lark-cli + 飞书生态完成主动知识服务。

当前代码已经从“会议 MVP”推进到“多源事项中枢骨架”：IM 分流、source ingestion、Hub 归属证据包、多 Hub 表结构和 guardrails 都已出现。下一步不应继续扩写设计文档，而应把 `pending_pull` 的来源逐个拉通，并用真实飞书资源完成 Demo 和效果验证。
